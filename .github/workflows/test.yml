name: Hello World Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Create cache directory
        run: mkdir -p ~/.cache/test-deps

      - name: Prime dependency cache
        id: prime-cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/test-deps
          key: cache-61d9ebd

      - name: prime-cache-61d9ebd
        run: echo "Cache hit? ${{ steps.prime-cache.outputs.cache-hit }}"

      - name: Hello World
        run: echo "Hello, world!"

      - name: 23f2005144@ds.study.iitm.ac.in
        run: echo "Hello, world!"
