name: Hello World Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Hello World
        run: echo "Hello, world!"

      - name: 23f2005144@ds.study.iitm.ac.in
        run: echo "Hello, world!"
